git+https://github.com/plotly/dash-deck.git  #xhlulu this is causing an error when installing requirements locally, but the app still runs (DannyB)
dash==1.18.1 #The latest Dash before Dash2.0
Werkzeug==2.0.0 #needed due to a Runtime error related to Werkzeug (assume from Dash2.0 updates done to it lately) error ImportError: cannot import name 'get_current_traceback' from 'werkzeug.debug.tbtools' (StackOverflow)
dash-bootstrap-components==0.13.1 #the latest before dash 2.0 breaking changes come into effect
gunicorn==20.0.4
networkx==2.8
pandas==1.3.5
geopandas==0.10.2
numpy==1.22.3
pydeck==0.5.0 #Fixed this. Any later version throws an error at runtime. Extra Dloads don't start
tqdm==4.64.0
requests==2.27.1